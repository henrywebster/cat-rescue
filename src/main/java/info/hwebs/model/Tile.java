package info.hwebs.model;

import javafx.scene.image.ImageView;
import javax.sound.sampled.Clip;

public final class Tile extends AbstractModel {

    Tile(double x, double y, ImageView view, Clip clip) {
        super(x, y, view, clip);
    }
}
